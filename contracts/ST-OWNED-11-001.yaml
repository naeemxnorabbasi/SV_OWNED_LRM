owned_id: OWNED-11-001
st_id: ST-OWNED-11-001
lrm_clause_id: LRM-CASE-INSIDE
lrm_section: "11.8"
title: case ... inside

owned_must:
  - Parse unique/priority case (expr) inside { set } in always_comb.
  - Export case-arm blocking assigns to procedural IR and connectivity fanout.

oracle:
  driver: slang
  parity_min: 0.99
  corpus_stems:
    - case_inside

tests:
  m5_test_id: test_sv_m5_309_owned_case_inside_parse
  feature_test_stem: case_inside

implementation:
  pod: parser_always
  cpp_files:
    - sv_frontend/cpp/compiler/src/parser.cpp

promotion:
  from_status: partial
  to_status: supported
  matrix_ids:
    - LRM-CASE-INSIDE
